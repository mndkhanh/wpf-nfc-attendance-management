<UserControl x:Class="WPF.Pages.FinancePage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="16" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel>
                <TextBlock Style="{StaticResource PageTitleStyle}"
                           Text="Quản lý tài chính" />
                <TextBlock Margin="0,6,0,0"
                           Foreground="{StaticResource MutedBrush}"
                           Text="Các khoản thu / chi và phí thành viên" />
            </StackPanel>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">
                <Button Margin="0,0,10,0"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="AddIncomeButton_Click"
                        Content="+ Thu" />
                <Button Margin="0,0,10,0"
                        Style="{StaticResource AccentButtonStyle}"
                        Click="AddExpenseButton_Click"
                        Content="+ Chi" />
                <Button Style="{StaticResource AccentButtonStyle}"
                        Click="AddMemberFeeButton_Click"
                        Content="+ Thu phí thành viên" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.2*" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                    Style="{StaticResource CardBorderStyle}"
                    Margin="0,0,12,0">
                <StackPanel>
                    <TextBlock Foreground="{StaticResource MutedBrush}"
                               Text="Tổng số dư hiện tại" />
                    <TextBlock Margin="0,8,0,0"
                               FontSize="28"
                               FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}"
                               Text="₫ 8.200.000" />
                    <TextBlock Margin="0,10,0,0"
                               Foreground="{StaticResource MutedBrush}"
                               Text="Cập nhật lần cuối 26/03/2026 19:20" />
                </StackPanel>
            </Border>

            <Border Grid.Column="1"
                    Style="{StaticResource CardBorderStyle}"
                    Margin="0,0,12,0"
                    Background="#F4FBF5">
                <StackPanel>
                    <TextBlock Foreground="{StaticResource MutedBrush}"
                               Text="Thu trong tháng" />
                    <TextBlock Margin="0,8,0,0"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="{StaticResource SuccessBrush}"
                               Text="+ ₫ 5.400.000" />
                </StackPanel>
            </Border>

            <Border Grid.Column="2"
                    Style="{StaticResource CardBorderStyle}"
                    Background="#FFF5F3">
                <StackPanel>
                    <TextBlock Foreground="{StaticResource MutedBrush}"
                               Text="Chi trong tháng" />
                    <TextBlock Margin="0,8,0,0"
                               FontSize="24"
                               FontWeight="Bold"
                               Foreground="{StaticResource DangerBrush}"
                               Text="- ₫ 4.100.000" />
                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="4"
                Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Style="{StaticResource SectionTitleStyle}"
                           Text="Báo cáo các khoản mục tài chính" />

                <DataGrid x:Name="FinanceGrid"
                          Grid.Row="1"
                          Style="{StaticResource AppDataGridStyle}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="120" Header="Ngày" Binding="{Binding TransactionDate}" />
                        <DataGridTextColumn Width="160" Header="Danh mục" Binding="{Binding Category}" />
                        <DataGridTextColumn Width="340" Header="Diễn giải" Binding="{Binding Description}" />
                        <DataGridTemplateColumn Width="140" Header="Số tiền">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock FontWeight="Bold"
                                               Foreground="{Binding AmountBrush}"
                                               Text="{Binding AmountDisplay}"
                                               VerticalAlignment="Center" />
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>

                <TextBlock Grid.Row="2"
                           Margin="0,10,0,0"
                           Foreground="{StaticResource MutedBrush}"
                           Text="- Cột nào là thu thì để dấu + | cột nào là chi thì để dấu -" />
            </Grid>
        </Border>
    </Grid>
</UserControl>
